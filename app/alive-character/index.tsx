import React from "react";
import { StyleSheet, Text, View } from "react-native";

const AliveCharacters = () => {
  return (
    <View>
      <Text>AliveCharacters</Text>
    </View>
  );
};

export default AliveCharacters;

const styles = StyleSheet.create({});
